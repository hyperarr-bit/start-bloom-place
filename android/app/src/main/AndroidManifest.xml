<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Backup do Google desligado (25/07): o auto-backup leva o localStorage
         do WebView junto, e é lá que mora o supabase.auth.token. Restaurar o
         backup noutro aparelho restauraria a SESSÃO logada junto. -->
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
            RETRATO TRAVADO (26/07). Sem isto o app girava, e em paisagem:
            - as telas viram scroll infinito (o layout é desenhado pra 430pt
              de largura e ~880 de altura)
            - o teclado do Android entra em "extract mode" e cobre a tela com
              um campo de texto branco em tela cheia — era o "quando clico em
              digitar fica bugado"
            - os cards do tutorial encavalam nas abas
            windowSoftInputMode=adjustResize faz a webview encolher quando o
            teclado sobe, em vez de o conteúdo ficar por baixo dele.
        -->
        <activity
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
                DEEP LINK por esquema próprio (27/07). `core://financas` abre o
                app já no módulo. Esquema próprio e não https:// de propósito:
                link https exigiria hospedar o assetlinks.json e verificação de
                domínio, e o único consumidor destes links hoje é o PRÓPRIO
                aparelho (atalhos do ícone e notificações). Quando existir link
                de campanha que precise abrir o app pelo navegador, aí sim vale
                o App Link verificado.
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="core" />
            </intent-filter>

            <!-- Atalhos do toque longo no ícone -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
