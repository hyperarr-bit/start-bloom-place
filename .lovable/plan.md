

## Plano: Trocar ícone do plano Pro

### Alteração em `src/pages/Planos.tsx`

Substituir o ícone `Zap` (raio) por um **"C" estilizado** usando o ícone `Crown` do Lucide — remete a "premium/pro" e combina com CORE.

**Alternativa**: usar `Gem` (diamante) ou `Shield` para transmitir exclusividade.

**Mudanças:**
- Linha 3: trocar `Zap` por `Crown` no import
- Linha 245: trocar `<Zap>` por `<Crown>`

Uma única alteração em um arquivo.

