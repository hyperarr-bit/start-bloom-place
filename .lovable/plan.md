Aumentar a espessura da borda preta (bezel) do iPhone CSS:

- Em `src/index.css`, na classe `.iphone-bezel`, aumentar o `padding` de `0.6%` para cerca de `1.4%`, deixando a faixa preta entre a moldura metálica e a tela mais visível, sem voltar ao tamanho exagerado anterior.
- Manter todos os outros valores (border-radius, frame, botões) iguais.
- Sem alterações no componente ou no comportamento do vídeo.