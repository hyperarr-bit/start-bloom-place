## Mudança

Trocar o estado inicial de `activeTab` em `src/pages/Index.tsx` (linha 46) de `"dashboard"` para `"financeiro"`. O app abre direto no Meu Financeiro, sem pulo intermediário.

Nada mais é alterado — a aba Dashboard continua existindo e acessível, só não é mais a padrão.
